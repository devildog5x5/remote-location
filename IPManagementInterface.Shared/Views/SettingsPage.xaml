<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IPManagementInterface.Shared.Views.SettingsPage"
             Title="Settings"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ScrollView>
        <StackLayout Padding="20" Spacing="15">
            
            <Frame BackgroundColor="{DynamicResource SurfaceColor}" 
                   CornerRadius="10" 
                   Padding="15">
                <StackLayout Spacing="10">
                    <Label Text="Theme" 
                           FontSize="20" 
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}"/>
                    
                    <Button Text="☀️ Light Theme" 
                            BackgroundColor="{DynamicResource PrimaryColor}"
                            Clicked="LightTheme_Clicked"/>
                    <Button Text="🌙 Dark Theme" 
                            BackgroundColor="{DynamicResource PrimaryColor}"
                            Clicked="DarkTheme_Clicked"/>
                </StackLayout>
            </Frame>

            <Frame BackgroundColor="{DynamicResource SurfaceColor}" 
                   CornerRadius="10" 
                   Padding="15">
                <StackLayout Spacing="10">
                    <Label Text="About" 
                           FontSize="20" 
                           FontAttributes="Bold"
                           TextColor="{DynamicResource PrimaryTextColor}"/>
                    <Label Text="IP Management Interface v1.0" 
                           TextColor="{DynamicResource SecondaryTextColor}"/>
                    <Label Text="Manage your IoT devices from your iPhone" 
                           TextColor="{DynamicResource SecondaryTextColor}"/>
                </StackLayout>
            </Frame>

        </StackLayout>
    </ScrollView>
</ContentPage>
